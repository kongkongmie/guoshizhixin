# 20260918丨果实之心丨模型联动与更新页
> 更新于 2026-09-18 · 状态：完成，用户实际连接切换待验收

## 结论
- 发布 20260918.4：模型联动默认开启，设置中可关闭；更新入口打开单独页面，支持离线记录、在线检查和校验后下载。
- 本轮用户明确授权直接改 Git；未读取或写入用户当前预设正文，也未改本地预设 JSON 或 settings.json。
- 13 项模型识别与别名分组测试通过；隔离浏览器验证四模型切换、关闭面板时联动、开关持久化、未知模型不写入、手动选择不反复覆盖、条目正文不变、更新下载及错误校验不覆盖缓存。

## 关键决策
- 每秒只读连接模型与预设名，签名变化稳定后才取预设并复用 applyTag；不在每次轮询重算/重写整份预设。首次载入和重新开启联动也会匹配当前连接。
- Gemini 3.7F、3.8F、FLASH（不区分大小写）统一归到 GEMINI FLASH，Pro 同理归到 GEMINI PRO；菜单与联动共用 modelGroup。appliedTag 返回条目原始标签，保留 tagStates 中每条原来的开关，不重命名或修改条目正文。浏览器验证菜单仅四项、Flash/Pro 分离和切回来保留原开关。
- 联动偏好存在浏览器 localStorage。用户手动切换面板模型组后，同一连接不再次强制覆盖，直到实际模型改变。
- 更新页采用现有 loader 缓存键及格式，已安装旧 loader 也能使用手动下载的新版本。不热替换运行中的面板，下载后刷新；本地测试副本需要用户主动切回 Git 版。

## 坑
- 安装版本保存为不可变 release 文件；先完成候选测试再 build。已生成的 release 内容不同会拒绝覆写，已发布版本不得改动。
- 主体模板在 src/fruit-heart.template.js，模型与更新模块分别插入；release-notes.json 同时生成页面内置记录、版本和 release.json，避免三处维护版本。
- 字节校验基于 LF 发布内容，必须先统一换行再计算 SHA-256。
- 连接名称相同但外部供应商改变不需要重切模型组；自定义的不含模型家族的别名不会猜测。

## 下一步
- 用户使用 Git 版刷新；缓存会后台更新，必要时再刷新一次，确认 v20260918.4。
- 在实际插头连接切换 Claude / Gemini Flash / Pro / DeepSeek，验证预设模型组；特殊自定义别名若需要支持，应按用户提供的真实名称补充映射。
